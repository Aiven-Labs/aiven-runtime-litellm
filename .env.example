# Local demo only. Generate independent values; never commit .env.
# Use openssl rand -hex 24 for POSTGRES_PASSWORD and UI_PASSWORD.
POSTGRES_PASSWORD=
UI_USERNAME=admin
UI_PASSWORD=
# Prefix a new openssl rand -hex 32 result with sk- for each key.
LITELLM_MASTER_KEY=
LITELLM_SALT_KEY=
